package com.ems.mappers;

import com.ems.entities.Room;
import com.ems.responsedto.RoomServiceResponseDTO;

public class RoomMapper {
    
    public static RoomServiceResponseDTO mapToRoomServiceResponseDTO(Room room){
        RoomServiceResponseDTO dto = new RoomServiceResponseDTO();

        // have to implement the mapping

        return dto;
    }
}
